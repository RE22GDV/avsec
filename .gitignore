# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Key material must never be committed
*.key
*.secret
*.pem
session_state*.json

# Working outputs: reproduce them with the commands in README.md.
# The curated evidence that backs the report lives in results/ and IS tracked.
runs/
reports/
data/generated/
# Natural source photographs: fetched with scripts/fetch_natural_sources.py,
# pinned by data/real/sources.json (URL + licence + sha256). 63 MB of images
# do not belong in a source repository; the registry that identifies them does.
data/real/natural/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
